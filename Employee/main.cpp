class student{
    public://gotta define or will be executed as private
    string name 
    Student (string n, int i, vector<double> g){ //constructor
        cout<<"constructer called"
        name = n
        setId(i)
        for(int i = 0; i<g.size();, i++)
    }
    void setName(string, n){
        name = n;
    }
    
    
    }
    int id 
    vector<double> grades;
    vector<double> getGrades{
    return grades;
    }
    
}

int main{
    vector<Student> students
    stundet s(Bob, 800, temp);
    //the constucter gets rid of the need for s. senarios 
    s.name = "bob"
    s.id = 80000000
    vector<doubles> temp = {4, 3, 2, 1}
    s.grades = temp
    //puch back to put into the class 
    //you can call the class in cout 
    //name, hourly, position 
    // 
    cout<<students.at[i].name
}